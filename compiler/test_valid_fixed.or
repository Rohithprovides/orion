fn main() { 
    # Test nested expressions and other combinations
    list1 = [1, 2]
    list2 = [3, 4]
    nested = (list1 + list2) + [5, 6]
    out(len(nested))  # Should be 6
    
    repeated = 2 * [10, 20] 
    out(len(repeated))  # Should be 4
    
    out("All valid operations work")
}
