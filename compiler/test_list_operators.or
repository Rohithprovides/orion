fn main() { 
    list1 = [1, 2]
    list2 = [3, 4]
    combined = list1 + list2
    out(len(combined))
    out(combined[0])
    out(combined[3])
    
    repeated = list1 * 3
    out(len(repeated))
    out(repeated[0])
    out(repeated[5])
}
