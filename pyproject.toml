[project]
name = "orion-web-ide"
version = "1.0.0"
description = "Web-based IDE for the Orion Programming Language - a pure compiled systems language"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.2",
    "flask-cors>=6.0.1",
    "gunicorn>=23.0.0",
]
